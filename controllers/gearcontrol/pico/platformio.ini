[env:pico]
platform = https://github.com/maxgerhardt/platform-raspberrypi.git
board = pico
framework = arduino
board_build.core = earlephilhower

; Shared ScaleFX libraries
lib_extra_dirs = ../../lib

; Note: Servo support is built into the earlephilhower core, no external lib needed
lib_ignore = 
    Servo
    sfx_usb
    sfx_audio
    sfx_storage
    esp_cdc_acm

monitor_speed = 6000000

; Upload over USB (mass storage)
upload_protocol = picotool

; Build flags for USB configuration
build_flags = 
    -DCFG_TUSB_CONFIG_FILE=\"tusb_config.h\"
    -DUSE_TINYUSB=1
    -DSCALEFX_SERVER=1

; Monitor settings
monitor_filters = 
    default
    log2file
